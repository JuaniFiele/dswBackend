{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "rm -rf node_modules && pnpm install --frozen-lockfile --shamefully-hoist && pnpm run build",
    "installCommand": "pnpm install --frozen-lockfile --shamefully-hoist"
  }
}